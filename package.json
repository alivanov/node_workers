{
  "name": "words_counter",
  "version": "1.0.0",
  "description": "A Node.js application to count the most frequently used words in text files from a specified directory, with multithreading support.",
  "main": "dist/index.js",
  "scripts": {
    "start": "npm run build && node dist/index.js",
    "build": "tsc",
    "lint": "tslint -p tsconfig.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "ivanov@intspirit.com",
  "license": "ISC",
  "engines": {
    "node": ">=14.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.5.1",
    "tslint": "^6.1.3",
    "typescript": "^5.5.4"
  }
}
